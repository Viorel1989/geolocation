extends layout 
 
block page 
 
  form.form-signin(method='post' action='/users/login')
    h1.h3.mb-3.font-weight-normal Please sign in
    label.sr-only(for='email') Your e-mail
    input#inputEmail.form-control(type='email' name='email' placeholder='Your e-mail address' required='' autofocus='')
    label.sr-only(for='password') Your Password
    input#inputPassword.form-control(type='password' name='password' placeholder='Password' required='')
    button.btn.btn-lg.btn-primary.btn-block(type='submit' value='Log In') Sign in
    
    if loginError   
      div.alert.alert-danger.mt-2(role='alert') Invalid email or password
    
    p Sign up #[a(href="/users/register") here]
